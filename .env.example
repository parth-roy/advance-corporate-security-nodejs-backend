# ============================================================
# ACS Backend — Environment Variables
# Copy to .env and fill in values before running
# NEVER commit .env to git — it is in .gitignore
# ============================================================

# Node Environment
NODE_ENV=production
PORT=4000
HOST=0.0.0.0

# ─── MongoDB Atlas ─────────────────────────────────────────
# Get from: https://cloud.mongodb.com → Your cluster → Connect → Drivers
MONGO_URI="mongodb+srv://<username>:<password>@acs.jgngoxc.mongodb.net/?appName=acs"

# ─── Frontend URL (CORS whitelist) ─────────────────────────
FRONTEND_URL=https://advancecorporatesecurity.com

# ─── Contact Details ───────────────────────────────────────
CONTACT_EMAIL=advancedcorporatesecurityj@gmail.com
CONTACT_PHONE="+91 93399 88999 / +91 79801 47044 / +91 94770 06681"

# ─── SMTP (Gmail App Password) ─────────────────────────────
# Setup steps:
# 1. Go to your Gmail account → Settings → Security → Enable 2-Step Verification
# 2. Go to Google Account → Security → App Passwords (under 2-Step Verification)
# 3. Select app: Mail, device: Windows Computer → Generate
# 4. Copy the 16-character password (without spaces) and paste below
SMTP_USER=advancedcorporatesecurityj@gmail.com
SMTP_PASS=your-16-char-gmail-app-password

# ─── DigitalOcean Spaces (S3-compatible) ───────────────────
# Used for media and document uploads
DO_SPACES_ENDPOINT=https://sgp1.digitaloceanspaces.com
DO_SPACES_BUCKET=acs-bkp-do
DO_SPACES_ACCESS_KEY=DO801WWKNR8CJBRHNR3P
DO_SPACES_SECRET_KEY=Y1SEQBcoD3n/MKxk4DXyL/0unSN02+LY9DRLeDUNmR4

# ─── Google Sheets Webhook (Zero-GCP Apps Script Sync) ───────
# Created via Google Sheets → Extensions → Apps Script → Deploy as Web App
GOOGLE_SHEETS_WEBHOOK_URL="https://script.google.com/macros/s/YOUR_APPS_SCRIPT_DEPLOYMENT_ID/exec"

